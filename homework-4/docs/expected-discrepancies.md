# Expected Discrepancies — Answer Key (NOT pipeline input)

> ⚠️ **Цей файл не є частиною контексту пайплайну.**
> Він навмисно лежить поза `context/bugs/001/`, щоб жоден агент його не прочитав.
> Призначення — оцінити роботу Research Verifier: скільки із закладених
> розбіжностей він знайшов і чи не забракував правильні твердження.

`context/bugs/001/research/codebase-research.md` містить **13 перевірюваних
тверджень**, з них **4 навмисно хибні**. Кожна помилка відтворює окремий
типовий режим галюцинації мовної моделі.

---

## D-1 — Зсунутий номер рядка

| | |
|---|---|
| **Де** | R-1, поле `Location` |
| **Твердження** | `src/cart.js:9` |
| **Дійсність** | `src/cart.js:8` |
| **Режим галюцинації** | сусідній правдоподібний рядок; типово для моделі, яка відтворює структуру файлу з пам'яті, а не читає його |
| **Що перевіряє** | чи верифікатор реально відкриває файл, а не приймає координати на віру |

---

## D-2 — Спотворена цитата коду

| | |
|---|---|
| **Де** | R-1, блок коду |
| **Твердження** | `total = total + item.price;` |
| **Дійсність** | `total += item.price;` |
| **Режим галюцинації** | семантично еквівалентний, але текстуально інший код — модель переказує сенс замість цитування |
| **Що перевіряє** | чи звіряється **текст** сніпета, а не лише номер рядка. Найлегше пропустити, бо сенс збігається |

---

## D-3 — Правдоподібний вимисел

| | |
|---|---|
| **Де** | R-4 повністю |
| **Твердження** | `applyDiscount` округлює результат через `Math.round(result * 100) / 100` |
| **Дійсність** | у `src/cart.js` немає ні `Math.round`, ні будь-якого округлення. Уся функція — це `return subtotal - percent;` |
| **Режим галюцинації** | опис коду таким, яким він **зазвичай буває**, а не яким є. Округлення до копійок — стандартна практика в грошових розрахунках, тому твердження не викликає підозри |
| **Що перевіряє** | чи відрізняє верифікатор прочитане від домисленого. Найнебезпечніший тип: у R-4 навіть немає поля `Location`, і саме його відсутність має насторожити |
| **Наслідок, якщо пропустити** | Bug Fixer «збереже» неіснуючу поведінку й додасть округлення, якого ніхто не просив |

---

## D-4 — Посилання на неіснуючий файл

| | |
|---|---|
| **Де** | R-6 повністю, а також рядок `src/validators.js:12` у секції References |
| **Твердження** | існує `src/validators.js` з функцією `isSafeIdentifier` |
| **Дійсність** | файлу `src/validators.js` у проєкті немає. У `src/` лише `cart.js`, `coupons.js`, `server.js` |
| **Режим галюцинації** | вигаданий модуль із правдоподібною назвою — модель добудовує архітектуру, якої очікує |
| **Що перевіряє** | чи перевіряється **існування файлу** перед цитуванням його вмісту |
| **Наслідок, якщо пропустити** | план виправлення посилатиметься на неіснуючий API, і Bug Fixer застрягне або вигадає його сам |

---

## Правильні твердження (не повинні бути забраковані)

Хибнопозитивне спрацювання — теж помилка верифікатора. Ці дев'ять тверджень
відповідають джерелам і мають пройти перевірку:

| Твердження | Посилання | Статус |
|---|---|---|
| R-1 (суть: `quantity` ігнорується) | — | ✅ по суті вірно, попри D-1 і D-2 |
| R-2 перевірка типу | `src/cart.js:2` | ✅ |
| R-3 формула знижки | `src/cart.js:14` | ✅ цитата точна |
| R-5 відсутня валідація діапазону | `src/cart.js:13` | ✅ (13 — рядок сигнатури функції, прийнятне посилання) |
| R-7 побудова шляху | `src/coupons.js:8` | ✅ цитата точна |
| R-8 `DATA_DIR` | `src/coupons.js:5` | ✅ цитата точна |
| R-9 `code` з рядка запиту | `src/server.js:31` | ✅ цитата точна |
| R-10 базлайн 12 тестів / 5 pass / 7 fail | `npm test` | ✅ |

---

## Як оцінювати Research Verifier

| Метрика | Як рахувати | Ціль |
|---|---|---|
| **Recall** | скільки з D-1…D-4 знайдено | 4 / 4 |
| **Precision** | скільки з дев'яти правильних тверджень помилково забраковано | 0 |
| **Найважче** | D-2 (спотворена цитата) і D-3 (вимисел) | обидва мають бути знайдені |
| **Оцінка якості** | рівень із `skills/research-quality-measurement.md` | має **не** бути найвищим — у дослідженні 4 дефекти |

Остання метрика — найпоказовіша. Якщо верифікатор знайде всі чотири
розбіжності, але все одно поставить дослідженню найвищий рівень якості,
значить рубрикатор написаний погано: він не пов'язує кількість знахідок із
підсумковою оцінкою.
