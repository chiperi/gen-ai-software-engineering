# Disable rate limiting for tests by default so per-IP counters don't leak across test
# classes. The dedicated RateLimitTest re-enables it with a low limit in its own context.
ratelimit.enabled=false
