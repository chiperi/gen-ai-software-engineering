# skills/

Reusable skills — procedures the agent applies on a trigger.
Format: a `<skill-name>/SKILL.md` directory with frontmatter `name` + `description`
(the description decides when the skill activates).
